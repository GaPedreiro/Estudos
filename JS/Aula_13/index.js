let umaString ='Gabriel Ramos'

/* o "charAt" é uma das funções que o JS tem pra trabalhar com Strings,
   chamamos de template string, ele apresenta o conteúdo do índice que
   você passou. */

console.log(umaString.charAt(0))

/* Nesse outro exemplo ele faz o contrário, ele retorna o valor do índex
    em que a letra 'G' está alocada. */
    
console.log(umaString.indexOf('G'))