const alunos = ['Gabriel', 'Camila', 'Zoe']
//alunos[alunos.length] = 'Frida' //Adiciona o nome 'Frida' ao final do array
//alunos.push('Caio') //Adiciona o nome Caio no final do array.
//alunos.unshift('Licy') //Adiciona no começo do array
//alunos.pop() //Exclui o último elemente do array, ele pode ser armazenado em uma variável.
//const removido = alunos.pop()
//const removido = alunos.shift() //Exclui o primeiro elemento do array
//delete alunos[1] //Deleta o elemento do índice 1, essa ação não altera os índices do array, então vai ficar um elemento vazio no índice 1.
//console.log(alunos.slice(0,2)) // O slice apresenta o conteúdo do array dentro do intervalo que você passar, inform primeiro o index inicial e depois o final, vale lembrar que o último não é exibido, então acrescente um a mais dependendo.
//console.log(alunos.slice(0. -1)) // Se você passa valor negativo, ele retorna somente os índices que seriam subtraídos, se eu passar -1, retorna só a Zozo


console.log(alunos.slice(0,2))

//console.log(removido)

//console.log(alunos.length) Apresenta o tamanho do array.

