/* A diferença entre let e const, é que quando utilizamos o let, não
podemos re-declarar a variável com o mesmo nome, podemos atribuir outro
valor a ela, mas não podemos declarar o nome dela novamente. No const,
uma vez que a variável foi declarada, o valor original dela não pode ser
alterado, ela sempre o manterá original */

// Tipo de dados primitivos: string, number, undefined, null, boolean, symbol